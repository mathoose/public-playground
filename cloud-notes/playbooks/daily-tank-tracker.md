# Daily tank tracker is the baseline

Do **not** build extra apps, boards, or preview sites to test tracker work.

Canonical file (work PC):

`C:\Users\M341004\Projects\tracker-system-v2-complete\daily-tank-tracker.html`

This Cloud Agent on `public-playground` cannot open that `file:///` path. If the user wants Post-it import or batch-sheet todos automated, they must open that folder in Cursor (desktop or a cloud environment that includes this repo).

`IMPORT ZIP` is a full backup restore. Never feed it a tasks-only file.
